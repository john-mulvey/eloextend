% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_calculation.R
\name{calculate_all_ratings}
\alias{calculate_all_ratings}
\title{Calculate All Ratings from Game Data}
\usage{
calculate_all_ratings(
  data,
  initial_rating = 1000,
  K = 32,
  method = "exponential",
  alpha = 1.4,
  D = 400
)
}
\arguments{
\item{data}{Data frame with columns: game_id, date, and one column per player
containing their rank in each game (1 = winner, NA = did not play)}

\item{initial_rating}{Numeric starting Elo rating for all players. Default is 1000.}

\item{K}{Numeric K-factor controlling rating volatility. Default is 32.}

\item{method}{Character string: "linear" or "exponential" scoring. Default is "exponential".}

\item{alpha}{Numeric exponent for exponential scoring. Default is 1.4.
For 2-player games, alpha has no effect (winner always gets score 1, loser gets 0).}

\item{D}{Numeric scale parameter for Elo calculation. Default is 400.}
}
\value{
List with two elements:
\itemize{
\item final_ratings: Named numeric vector of final Elo ratings
\item ratings_history: Data frame with columns (date, player, rating, game_id)
}
}
\description{
Calculates Elo ratings for all players across a series of games.
Returns both final ratings and complete rating history.
}
\details{
This function processes games sequentially, updating ratings after each game
using the update_ratings() function. It can be used for initial calculation
or for incremental updates by providing a subset of games.
}
\examples{
# Three-player game example demonstrating multiplayer Elo
game_data <- data.frame(
  game_id = 1:3,
  date = as.Date(c("2024-01-01", "2024-01-02", "2024-01-03")),
  Alice = c(1, 2, 1),
  Bob = c(2, 1, 3),
  Charlie = c(3, 3, 2)
)
results <- calculate_all_ratings(game_data)
print(results$final_ratings)
head(results$ratings_history)
}
